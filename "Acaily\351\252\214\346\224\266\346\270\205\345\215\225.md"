# Acaily 验收清单 + UAT 用例

> 对齐 `Acaily需求说明书.md` 验收标准与 `Acaily实施计划.docx` M6。

## 一、功能验收（对照需求）

| 编号 | 验收项 | 通过标准 | 状态 |
|---|---|---|---|
| A1 | 对话交互（多轮） | 用户经飞书发消息，模型按配置回复，连续多轮上下文连贯 | ☐ |
| A2 | 用户自配模型（核心差异化） | 每位用户可在设置页配置各自 Provider/URL/API Key/Model 并独立生效 | ☐ |
| A3 | 密钥安全 | API Key 入库即加密，接口读取脱敏，明文不出服务端、不落盘 | ☐ |
| A4 | 多 Provider 适配 | OpenAI 兼容 / Anthropic / Ollama / 自建网关 四类均可对话 | ☐ |
| A5 | 技能/工作流（MCP） | Agent 可调用外部 MCP 工具并返回结果 | ☐ |
| A6 | 知识库 RAG | 入库文档后，相关提问能命中并带引用溯源 | ☐ |
| A7 | 飞书生态接入 | 事件回调签名校验通过，消息双向收发正常 | ☐ |
| A8 | 限流/降级 | 高频调用被限流；下游失败时重试退避或降级 | ☐ |

## 二、安全与合规验收

| 编号 | 验收项 | 通过标准 |
|---|---|---|
| S1 | 信封加密 | KMS envelope（DEK+AES-256-GCM）加解密往返一致，篡改密文被拒 |
| S2 | 租户隔离 | 跨 open_id 读取配置/会话/历史均被拒绝（404/无权限） |
| S3 | 传输校验 | 飞书回调 X-Lark-Signature 校验，伪造请求 401 |
| S4 | 审计 | 配置变更/对话/密钥访问全链路审计可查 |
| S5 | 后台权限 | 管理后台需管理员令牌，无令牌 401 |

## 三、UAT 用例（客户侧走查）

1. **自配模型**：以用户 A 身份在 `/settings` 配置 OpenAI，以用户 B 配置 Ollama；各自对话验证模型不同。
2. **密钥安全**：配置后调用 GET /config，确认无明文 key（仅 hasApiKey 布尔）。
3. **连通性**：保存后点「连通性测试」，OpenAI 正常返回 ok，错误地址返回明确失败。
4. **知识库**：入库一篇产品文档，提问命中并出现 `[1]` 引用。
5. **限流**：连续高频请求，观察超阈值被拒并提示。
6. **后台**：管理员持令牌查看 /admin/stats 与 /admin/audit；普通请求无令牌返回 401。

## 四、遗留与待排期

- C7 等保/SOC2 外部评估与渗透测试（需第三方排期）
- C8 数据留存/销毁策略、用户数据导出与删除接口
- 生产级向量库（pgvector/Milvus）替换内存实现
- 真实 MCP server 接入与权限审批流
