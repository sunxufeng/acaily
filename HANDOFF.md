# Acaily 上线切换 & 客户成功交接（T6.3）

## 灰度与回滚

- **灰度**：先对内部试点用户开放 `/settings` 与对话；观察 /admin/stats、/admin/audit 一周无异常后全量。
- **回滚预案**：无状态服务，回滚即切回上一镜像版本；用户配置/会话/审计均在外部 JSON 存储（挂卷），与代码解耦，回滚不影响数据。
- **配置漂移**：`ACAILY_MASTER_KEY` 一旦设置不可更换（否则历史密文无法解密），轮换需先解密再重加密迁移。

## 监控与告警

- 存活：`GET /health`（容器 HEALTHCHECK 已配置）。
- 业务：对话成功率、限流触发率、下游 Provider 错误率（从 /admin/audit 的 chat.error 统计）。
- 安全：异常高频 key.decrypt、跨租户访问尝试告警。

## SLA 建议

- 对话 P95 < 8s（受下游模型影响）；限流阈值按套餐设定。
- 故障响应：P1（服务不可用）15 分钟内响应。

## 客户成功接手清单

1. 交付物：需求说明书、实施计划+架构图、本工程代码（acaily/）、验收清单、运维手册。
2. 培训：管理员后台使用、用户自配模型引导、常见问题 FAQ（见 docs/RUNBOOK.md）。
3. 后续运营：收集 Provider 接入反馈、推动 C7/C8（外部合规评估）、真实向量库与 MCP server 接入。
4. 知识沉淀：复盘与 FAQ 沉淀至乐享/项目资料库，供后续项目复用。
